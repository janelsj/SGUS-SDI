/*
    Use instructor's code as reference, attempt to try:
    - const and let
    - apply console.log to both initialized and uninitialized variable
*/

var age = 30;
console.log(age);

let number = 80;
console.log (number);

const name = 'computer';
console.log(name);