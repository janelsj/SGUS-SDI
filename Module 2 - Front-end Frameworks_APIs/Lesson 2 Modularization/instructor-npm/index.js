// 1. use npm init to setup package.json

// 2. Add {"type":"module"} and {"script":{"start":"node index.js"}}

// 3. Use this command in ternimal `npm install lodash`

import _ from "lodash";
const nums = _.range(1, 9);

console.log(nums);
